import Panel from '../components/Panel';

const Social = () => (
  <div className="page">
    <Panel title="Squads" variant="alt">
      <div className="notice">
        Social hub is on the roadmap. Reserve this space for squads, friends, and dispatch channels.
      </div>
    </Panel>
  </div>
);

export default Social;
