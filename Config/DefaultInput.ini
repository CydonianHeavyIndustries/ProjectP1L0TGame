[/Script/Engine.InputSettings]

; =====================================================
; PROJECT P1L0T – GLOBAL KEYBINDS (AUTHORITATIVE)
; =====================================================
; These bindings define the control language of the game.
; Many actions are placeholders and will be implemented later.
; DO NOT remove bindings even if unused yet.

; ---------------------
; MOVEMENT
; ---------------------
+AxisMappings=(AxisName="MoveForward",Scale=1.0,Key=W)
+AxisMappings=(AxisName="MoveForward",Scale=-1.0,Key=S)
+AxisMappings=(AxisName="MoveRight",Scale=1.0,Key=D)
+AxisMappings=(AxisName="MoveRight",Scale=-1.0,Key=A)

; Jump / parkour continuation (hold)
+ActionMappings=(ActionName="Jump",Key=SpaceBar)

; ---------------------
; WEAPONS
; ---------------------
; Weapon slots
+ActionMappings=(ActionName="WeaponSlot1",Key=One)
+ActionMappings=(ActionName="WeaponSlot2",Key=Two)
+ActionMappings=(ActionName="WeaponSlot3",Key=Three)
+ActionMappings=(ActionName="WeaponSlot4",Key=Four)

; Cycle weapons
+AxisMappings=(AxisName="CycleWeapon",Scale=1.0,Key=MouseScrollUp)
+AxisMappings=(AxisName="CycleWeapon",Scale=-1.0,Key=MouseScrollDown)

; Fire / Aim
+ActionMappings=(ActionName="Fire",Key=LeftMouseButton)
+ActionMappings=(ActionName="Aim",Key=RightMouseButton)

; Reload (tap) / Accessory wheel (hold)
+ActionMappings=(ActionName="Reload",Key=R)

; Melee
+ActionMappings=(ActionName="Melee",Key=F)

; Ordinance
+ActionMappings=(ActionName="Ordinance",Key=Q)

; ---------------------
; STANCE & SPEED
; ---------------------
+ActionMappings=(ActionName="Sprint",Key=LeftShift)

; Crouch / Slide (context-sensitive)
+ActionMappings=(ActionName="Crouch",Key=LeftControl)

; ---------------------
; ABILITIES
; ---------------------
; Primary class ability
+ActionMappings=(ActionName="ClassAction",Key=MiddleMouseButton)

; Secondary class ability
+ActionMappings=(ActionName="SecondaryClassAction",Key=Z)

; Deploy gadget
+ActionMappings=(ActionName="Gadget",Key=G)

; Boost / power-up
+ActionMappings=(ActionName="Boost",Key=C)

; ---------------------
; TITAN / BUDDY
; ---------------------
; Call Titan / Buddy
+ActionMappings=(ActionName="CallTitan",Key=V)

; Titan command radial (hold)
+ActionMappings=(ActionName="TitanCommand",Key=V)

; Confirm / Cancel while commanding
+ActionMappings=(ActionName="TitanCommandConfirm",Key=LeftMouseButton)
+ActionMappings=(ActionName="TitanCommandCancel",Key=RightMouseButton)

; ---------------------
; INTERACTION
; ---------------------
; Interact (tap) / Context wheel (hold)
+ActionMappings=(ActionName="Interact",Key=E)

; ---------------------
; UI & MENUS
; ---------------------
+ActionMappings=(ActionName="Menu",Key=Escape)
+ActionMappings=(ActionName="Map",Key=M)
+ActionMappings=(ActionName="Social",Key=P)
+ActionMappings=(ActionName="Chat",Key=Enter)

; Scoreboard (hold)
+ActionMappings=(ActionName="Scoreboard",Key=Tab)

; Inventory / loadout
+ActionMappings=(ActionName="Inventory",Key=I)

; Objectives / mission log
+ActionMappings=(ActionName="Objectives",Key=L)

; ---------------------
; COMMUNICATION
; ---------------------
; Proximity voice chat
+ActionMappings=(ActionName="ProximityVoice",Key=X)

; Team voice chat
+ActionMappings=(ActionName="TeamVoice",Key=T)

; Squad voice chat
+ActionMappings=(ActionName="SquadVoice",Key=Y)

; ---------------------
; META / FUTURE SYSTEMS
; ---------------------
+ActionMappings=(ActionName="Build",Key=B)
+ActionMappings=(ActionName="Emote",Key=H)
+ActionMappings=(ActionName="SkillTree",Key=K)
+ActionMappings=(ActionName="SettingsSubmenu",Key=O)

; ---------------------
; DEBUG / HELP
; ---------------------
+ActionMappings=(ActionName="Help",Key=F1)
+ActionMappings=(ActionName="NetStats",Key=F3)
+ActionMappings=(ActionName="ToggleUI",Key=F10)

; =====================================================
; END KEYBINDS
; =====================================================
